{
  "name": "kite-marketplace-workspace",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "start": "pnpm -r start",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "typescript": "^5",
    "pnpm": "^9"
  }
}
